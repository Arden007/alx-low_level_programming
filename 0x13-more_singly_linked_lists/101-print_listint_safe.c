#include "lists.h"
/**
 * print_listint_safe - define function
 * @head: pointer to the starting node of the
 * listint_t linked list
 * Return: 0 on success, -1 on failure
 */
size_t print_listint_safe(const listint_t *head)
{
head++;
return (0);
}
