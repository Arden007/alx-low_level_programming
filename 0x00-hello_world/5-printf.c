#include <stdio.h>
#include <string.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char phrase[] = "\"with proper grammar, but the outcome is a piece of art,";

printf(phrase);
return (0);
}
